import { PreviewFileDirective } from './preview-file.directive';

describe('PreviewFileDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviewFileDirective();
    expect(directive).toBeTruthy();
  });
});
