import { KhmerDatePipe } from './khmer-date.pipe';

describe('KhmerDatePipe', () => {
  it('create an instance', () => {
    const pipe = new KhmerDatePipe();
    expect(pipe).toBeTruthy();
  });
});
