import { PreviewPdfDirective } from './preview-pdf.directive';

describe('PreviewPdfDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviewPdfDirective();
    expect(directive).toBeTruthy();
  });
});
