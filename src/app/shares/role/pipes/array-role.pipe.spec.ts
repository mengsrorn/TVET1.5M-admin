import { ArrayRolePipe } from './array-role.pipe';

describe('ArrayRolePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayRolePipe();
    expect(pipe).toBeTruthy();
  });
});
