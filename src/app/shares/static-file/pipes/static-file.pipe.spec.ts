import { StaticFilePipe } from './static-file.pipe';

describe('StaticFilePipe', () => {
  it('create an instance', () => {
    const pipe = new StaticFilePipe();
    expect(pipe).toBeTruthy();
  });
});
