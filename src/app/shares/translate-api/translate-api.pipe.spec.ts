import { TranslateApiPipe } from './translate-api.pipe';

describe('TranslateApiPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateApiPipe();
    expect(pipe).toBeTruthy();
  });
});
